name: '[build] flatpak bundle'

on:
  push:
    tags: ['*']

permissions: {}

jobs:
  flatpak-validation:
    uses: ./.github/workflows/build-and-bundle.yml

  flatpak-bundle:
    name: '[build] create flatpak bundle'
    runs-on: ubuntu-latest
    needs: flatpak-validation

    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:gnome-49
      options: --privileged

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Setup Project
        uses: ./.github/actions/setup-project

      - name: Build Flatpak bundle
        uses: flatpak/flatpak-github-actions/flatpak-builder@v6.6
        with:
          build-bundle: true
          upload-artifact: true
          manifest-path: flatpak/io.github.andrepg.Doit.json
          bundle: io.github.andrepg.Doit.${{ github.ref_name }}.flatpak
          cache-key: flatpak-release-${{ github.ref_name }}

      - name: Upload bundle to release
        uses: softprops/action-gh-release@e2b1d7a6fd65ebda87147ce321d4c1fb37eb1d41
        if: github.ref_type == 'tag'
        with:
          draft: false
          tag_name: ${{ github.ref_name }}
          files: io.github.andrepg.Doit-${{ github.ref_name }}.flatpak
