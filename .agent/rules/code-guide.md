---
trigger: always_on
---

- This is a GNOME application. We use Typescript, Meson and Flatpak to build and redistribute
- Some scripts to run, compile and generate languages are in the `scripts` folder
- The languages are managed in the `po` folder, in a `gettext` format
- The comments must ALWAYS be written in English
